services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: horizon
      POSTGRES_PASSWORD: horizon
      POSTGRES_DB: horizon_dashboard
    ports:
      - "5432:5432"
    volumes:
      - horizon_pg_data:/var/lib/postgresql/data

volumes:
  horizon_pg_data:
